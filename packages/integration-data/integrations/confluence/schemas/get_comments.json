{
  "type": "object",
  "properties": {
    "pageId": {
      "type": "string",
      "title": "Page ID",
      "description": "The Confluence page ID."
    },
    "includeBodyFormat": {
      "type": "string",
      "enum": ["storage", "atlas_doc_format"],
      "default": "storage",
      "description": "Which body representation to request for comment bodies. Confluence API v2 expects PrimaryBodyRepresentation values: STORAGE or ATLAS_DOC_FORMAT."
    },
    "limit": {
      "type": "integer",
      "minimum": 1,
      "maximum": 250,
      "default": 50,
      "description": "Maximum number of comments to return."
    },
    "cursor": {
      "type": ["string", "null"],
      "description": "Pagination cursor from a previous response."
    }
  },
  "required": ["pageId"],
  "additionalProperties": false
}

