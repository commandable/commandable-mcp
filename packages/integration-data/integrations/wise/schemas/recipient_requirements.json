{
  "type": "object",
  "properties": {
    "quoteId": {
      "type": "string",
      "description": "Wise quote UUID whose route determines recipient requirements."
    }
  },
  "required": ["quoteId"],
  "additionalProperties": false
}
