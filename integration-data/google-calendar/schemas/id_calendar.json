{
  "type": "object",
  "properties": {
    "calendarId": { "type": "string", "description": "Calendar ID (use 'primary' for primary)." }
  },
  "required": ["calendarId"],
  "additionalProperties": false
}
