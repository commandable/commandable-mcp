Shared binary fixtures for integration tests across integrations (e.g. Google Drive upload + `read_file_content`, future attachment flows).

Each file should include the exact phrase **Commandable Integration Test** in extractable text so tests can assert extraction succeeded.

Expected files (replace empty placeholders with real Office/PDF binaries):

- `sample.docx`
- `sample.xlsx`
- `sample.pptx`
- `sample.pdf`
